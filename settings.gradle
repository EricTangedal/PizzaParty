pluginManagement {
    repositories {
        google()
        maven { url "https://maven.google.com" }
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url "https://maven.google.com" }
        mavenCentral()
    }
}
rootProject.name = "Pizza Party"
include ':app'
